<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="MembersDAO">
	<insert id="insertMembers" parameterType="members">
		insert into members(members_id, members_pw, members_name, members_image
		members_addr, members_tel, members_mail)
		values ( #{membersId}, #{membersPw}, #{membersName}, #{membersImage},
		 #{membersAddr}, #{membersTel}, #{membersMail})
	</insert>
	
	<!-- 단건조회 -->
	<select id="getMembers" parameterType="members" resultType="members">
		select * from members where members_id = #{membersId}
	</select>
</mapper>