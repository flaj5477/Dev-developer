<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="MembersDAO">
	<!-- <insert id="insertMembers" parameterType="members">
		insert into members(members_id, members_pw, members_name, members_image
		members_addr, members_tel, members_mail, members_type)
		values ( #{membersId}, #{membersPw}, #{membersName}, #{membersImage},
		 #{membersAddr}, #{membersTel}, #{membersMail},#{membersType})
	</insert> -->
	
	<!-- <insert id="insertMembers" parameterType="members">
		insert into members(members_no, members_id, members_pw, members_name, 
		members_addr, members_tel, members_mail, members_type)
		values ((select max(members_no)+1 from members), #{membersId}, #{membersPw}, #{membersName}, 
		 #{membersAddr}, #{membersTel}, #{membersMail},#{membersType})
	</insert> -->
	
		<insert id="insertMembers" parameterType="members">
		insert into members(members_no, members_id, members_pw, members_name 
		, members_type, members_mail 
		)
		values ((select max(members_no)+1 from members), #{membersId}, #{membersPw},
		 #{membersName} , #{membersType} , #{membersMail}
		 )
	</insert>
</mapper>